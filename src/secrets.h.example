#ifndef SECRETS_H
#define SECRETS_H

// ── Template pentru credentiale ─────────────────────────────────────────
// Copiaza acest fisier ca `secrets.h` (in acelasi folder) si completeaza
// valorile reale. `secrets.h` este ignorat de git, deci parolele nu ajung
// in repo. Daca `secrets.h` lipseste, WebControl.h foloseste valori default.

#define WIFI_SSID "TTROBOT"
#define WIFI_PASS "changeme"

// PIN pentru autorizarea update-ului de firmware (OTA).
#define OTA_PIN "0000"

#endif // SECRETS_H
